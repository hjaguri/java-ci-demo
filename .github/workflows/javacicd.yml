name: "JAVA PROGRAMMING CICD"

on:
    push:
        branches:
            - main
            - master

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: "Setup JDK"
              uses: actions/setup-java@v4
              with:
                java-version: '17'

            - name: "Checking Version"
              run: java --version